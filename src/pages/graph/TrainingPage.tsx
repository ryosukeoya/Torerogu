import React, { VFC } from 'react';
import { pageTemplate } from '~/styles/shares';

const TrainingPage: VFC = () => {
  return <p css={pageTemplate.contentArea}>作成中</p>;
};

export default TrainingPage;
